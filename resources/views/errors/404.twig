{% extends "layout.twig" %}

{% block title %}{{ 'errors.404.title'|trans }}{% endblock %}
{% block content %}
			<div class="jumbotron">
				<div class="container text-center">
					<span class="help">{{ 'errors.404.header'|trans }}</span>
					<h2>{{ 'errors.404.title'|trans }}</h2>
					<p>{{ 'errors.404.text'|trans }}</p>
					<p><a href="{{ path_for('index', {'locale': locale}) }}" class="btn btn-success"><i class="fa fa-arrow-left"></i> {{ 'base.back-home'|trans }}</a></p>
				</div>
			</div>
{% endblock %}
