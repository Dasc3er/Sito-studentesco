{% extends "layout.twig" %}

{% block title %}{{ 'index.title'|trans }}{% endblock %}
{% block content %}
            <div class="jumbotron">
				<div class="container text-center">
					<h1><i class="fa fa-graduation-cap"></i> {{ 'index.header'|trans }}</h1>
					<p>{{ 'index.text'|trans }}</p>
				</div>
			</div>
			<hr>
			<div class="container">
                <img class="img-responsive img-rounded" style="height:400px;margin: 0 auto;" src="{{ img.getUrl('logo.png') }}">
				<div class="row">
					<div class="col-xs-12 col-md-6"></div>
					<div class="col-xs-12 col-md-6"></div>
				</div>

                <h3 class="text-center">{{ 'index.inspire'|trans }}</h3>
			</div>
{% endblock %}
