{% extends "layout.twig" %}

{% block title %}{{ 'logins.title'|trans }}{% endblock %}
{% block content %}
			<div class="jumbotron">
				<div class="container text-center">
					<h1><i class="fa fa-wrench"></i> {{ 'logins.header'|trans }}</h1>
					<p>{{ 'logins.text'|trans }}</p>
				</div>
			</div>
			<div class="container">
				<table class="table table-hover scroll">
					<thead>
						<tr>
							<th>{{ 'base.username'|trans }}</th>
							<th>{{ 'base.info'|trans }}</th>
							<th>{{ 'base.IP-address'|trans }}</th>
							<th>{{ 'base.date'|trans }}</th>
						</tr>
					</thead>
					<tbody>
{% for result in results %}
						<tr>
							<td>{{ result.user.name }}</td>
							<td>{{ result.browser }}</td>
							<td>{{ result.address }}</td>
							<td>{{ result.created_at }}</td>
						</tr>
{% endfor %}
					</tbody>
				</table>
                {{ results.links()|raw }}
				<p>{{ 'logins.total'|trans }}: {{ count }}</p>
				<p><a href="{{ path_for('reset-accessi', {'locale': locale}) }}" class="btn btn-danger">{{ 'logins.reset'|trans }}</a></p>
			</div>
{% endblock %}
